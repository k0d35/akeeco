export const environment = {
  googlePlaces: {
    // Keep this false by default. Turn on in code when ready.
    enabled: true,
    pickupEnabled: true,
    dropoffEnabled: true,
    apiKey: 'TBD',
    minQueryLength: 3,
    maxSuggestions: 4,
    regionCodes: ['jm'],
  },
};
